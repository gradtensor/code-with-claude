# Day 2: Context management and the Model Context Protocol

A model is only as good as the context it sees. This day covers how to assemble context efficiently: what to include, what to summarise, what to leave out. It introduces the Model Context Protocol (MCP) as a standard way to expose tools and resources to Claude, and walks through connecting a simple MCP server to the analyst.

Status: not yet shipped.
