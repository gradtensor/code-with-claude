# Day 4: Agentic workflow patterns

Not every problem needs a single agent. This day surveys the patterns that show up in production: prompt chaining, routing, parallelisation, orchestrator-worker, evaluator-optimiser, and the reflection loop. Each pattern is implemented as a small runnable example and mapped to a use case in the capstone.

Status: not yet shipped.
