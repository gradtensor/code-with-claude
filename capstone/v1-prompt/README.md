# Capstone v1: prompt-only baseline

The first version of the AI Document Analyst. No tools, no agent loop, no retrieval - just a well-engineered prompt that takes a document and returns a structured analysis. This is the baseline every later version is measured against.

Ships with Post 1.
